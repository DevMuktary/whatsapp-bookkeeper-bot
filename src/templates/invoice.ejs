<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Helvetica', sans-serif; color: #333; padding: 40px; }
        .header { display: flex; justify-content: space-between; margin-bottom: 40px; border-bottom: 2px solid #eee; padding-bottom: 20px; }
        .company-info h1 { margin: 0; color: #2c3e50; font-size: 28px; }
        .invoice-label { text-align: right; }
        .invoice-label h2 { margin: 0; color: #7f8c8d; font-size: 24px; }
        .bill-to { margin-bottom: 30px; }
        .table { width: 100%; border-collapse: collapse; margin-bottom: 30px; }
        .table th { background: #f8f9fa; padding: 12px; text-align: left; border-bottom: 2px solid #ddd; }
        .table td { padding: 12px; border-bottom: 1px solid #eee; }
        .text-right { text-align: right; }
        .totals { float: right; width: 300px; }
        .totals-row { display: flex; justify-content: space-between; padding: 8px 0; }
        .grand-total { font-weight: bold; font-size: 18px; border-top: 2px solid #333; margin-top: 10px; padding-top: 10px; }
        .footer { clear: both; margin-top: 50px; text-align: center; font-size: 12px; color: #777; }
    </style>
</head>
<body>
    <div class="header">
        <div class="company-info">
            <h1><%= businessName %></h1>
            <p><%= email %></p>
        </div>
        <div class="invoice-label">
            <h2>INVOICE</h2>
            <p>#<%= transactionId.slice(-6).toUpperCase() %></p>
            <p>Date: <%= date %></p>
        </div>
    </div>

    <div class="bill-to">
        <strong>Bill To:</strong><br>
        <%= customerName %>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Item Description</th>
                <th class="text-right">Qty</th>
                <th class="text-right">Price</th>
                <th class="text-right">Total</th>
            </tr>
        </thead>
        <tbody>
            <% items.forEach(function(item) { %>
            <tr>
                <td><%= item.productName %></td>
                <td class="text-right"><%= item.quantity %></td>
                <td class="text-right"><%= currency %> <%= item.pricePerUnit.toLocaleString() %></td>
                <td class="text-right"><%= currency %> <%= (item.quantity * item.pricePerUnit).toLocaleString() %></td>
            </tr>
            <% }); %>
        </tbody>
    </table>

    <div class="totals">
        <div class="totals-row grand-total">
            <span>Total</span>
            <span><%= currency %> <%= totalAmount.toLocaleString() %></span>
        </div>
    </div>

    <div class="footer">
        <p>Thank you for your business!</p>
    </div>
</body>
</html>
